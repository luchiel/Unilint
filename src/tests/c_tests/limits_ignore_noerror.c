int main ( int argc, int* argv[] )
{
    //1st
    int meaningless_integer_with_extremely_long_meaningless_name_that_is_longer_than_80 = 0;
    int normal_integer_that_doesnt_violate_line_length_rule = 0;
    int a, b; //couple of short ints
    scanf("%d%d", &a, &b);

    if(a > b)
        //2nd
        if(a > b + 1)
        {
            //3rd
            for(; a != b + 1; a--)
                //4th
                if(a / 2 > b)
                {
                    //5th
                    int i = 0;
                    while(a / 2 - i > b)
                    {
                        //6th
                        printf("oh i really don't know what it does");
                    }
                }
        }


        int c;



        int d;
}