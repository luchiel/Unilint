function camelStyle: boolean;
function PascalStyle(var j: integ;er): boolean;
function underscore_style: boolean;
function CAPS_UNDERSCORE_STYLE: boolean;
var
  caMel: integer;
  Pascal: boolean;
  under_score: string;
  CAPS_UNDERSCORE: char;
begin
end;

procedure oCamel(x: integer);
procedure PascalMe;
procedure un_sco;
procedure CAPS_UN_SCO;

type
  camelRecord = record
    x, y: integer
  end;
  
  PascalClass = class
  private
    y: integer;
  public
    x: integer;
  end;
  
  underscore_type = integer;
  CAPS_UNDERSCORE_TYPE = boolean;

var
  caMel: integer;
  Pascal: boolean;
  under_score: string;
  CAPS_UNDERSCORE: char;
begin
end.