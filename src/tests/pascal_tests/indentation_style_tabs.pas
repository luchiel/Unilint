function with_lots_of_vars(i, j, k, l, m: integer; z, b, s: char;
	q, w, e: string): boolean;
begin
end;

var
	i, b: integer;
	c: string;
begin
	for i := 1 to 10 do begin
        //use spaces
        if b > i then
    		break;
		end;
end.